spring:
  application:
    name: shop-api

  datasource:
    url: jdbc:postgresql://postgres:5432/shop_dev
    username: ajamrich
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080